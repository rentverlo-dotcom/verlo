name: E2E Preview

on:
  deployment_status:
  workflow_dispatch:

jobs:
  e2e:
    if: >
      github.event_name == 'workflow_dispatch' ||
      (
        github.event.deployment_status.state == 'success' &&
        github.event.deployment_status.environment_url != ''
      )

    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      PREVIEW_BASE_URL: ${{ github.event_name == 'workflow_dispatch' && 'https://verlo.lat' || github.event.deployment_status.environment_url }}
      PROD_BASE_URL: https://verlo.lat

    steps:
      - name: Checkout deployed ref
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.deployment.ref || github.sha }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Smoke preview
        env:
          PLAYWRIGHT_BASE_URL: ${{ env.PREVIEW_BASE_URL }}
        run: npm run test:smoke

      - name: Full E2E production
        env:
          PLAYWRIGHT_BASE_URL: ${{ env.PROD_BASE_URL }}
        run: npm run test:e2e

      - name: Upload Playwright artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts
          path: |
            test-results
            playwright-report
          if-no-files-found: ignore
